{% extends 'base.html.twig' %}

{% block title %}Modifier véhicule{% endblock %}

{% block body %}
    <h1 class="ms-4">Modifier véhicule</h1>

    {{ include('car/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_car_index') }}" class="btn btn-success ms-4">Retour à la liste</a>

    {{ include('car/_delete_form.html.twig') }}
{% endblock %}
