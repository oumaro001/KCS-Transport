{{ form_start(form) }}
<style>




</style>
<div class="mx-auto" id="photo" style="width:80%;">
    {{ form_row(form.lastname) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.email) }}
    {{ form_row(form.adress) }}
    {{ form_row(form.zipcode) }}
    {{ form_row(form.city) }}
    {{ form_row(form.phone) }}
    {{ form_row(form.fonction) }}
    {{ form_row(form.imageFile) }}
    {{ form_row(form.car) }}
    {{ form_row(form.roles) }}

</div>
    <button class="btn btn-warning ms-5 mt-4 mb-3">{{ button_label|default('Modifier') }}</button>

{{ form_end(form) }}

